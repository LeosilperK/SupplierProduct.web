
  # SupplierProduct.web

  SupplierProduct.web is a web application for supplier product registration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  